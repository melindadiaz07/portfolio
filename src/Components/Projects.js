import React from 'react';




const Projects = () => {
  return (
    <div>
      <div className="side-tabs">
        {/* add image and short description for each icon tab.
        create a slected class to highlight the project they've clicked on
        conditionally render project component based on which one is selected (state) */}
      </div>
    </div>
  )
}

export default Projects;